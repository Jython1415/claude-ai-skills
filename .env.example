# Git Proxy Server Configuration

# Required: Secret key for authentication
PROXY_SECRET_KEY=your-secret-key-here

# Optional: Server port (default: 8443)
PORT=8443

# Optional: Flask proxy URL used by MCP server (must match PORT above)
FLASK_URL=http://localhost:8443

# Optional: Debug mode (default: false)
DEBUG=false

# Public URL for the proxy server (returned in session responses for Claude.ai scripts)
# This should be the Cloudflare Tunnel URL that routes to the Flask proxy (port 8443)
PUBLIC_PROXY_URL=https://proxy.yourdomain.com

# GitHub OAuth Configuration (for MCP Server)
# Create OAuth App at: https://github.com/settings/developers
# Callback URL: https://mcp.yourdomain.com/oauth/callback
GITHUB_CLIENT_ID=your-github-oauth-client-id
GITHUB_CLIENT_SECRET=your-github-oauth-client-secret
GITHUB_ALLOWED_USERS=your-github-username
BASE_URL=https://mcp.yourdomain.com
